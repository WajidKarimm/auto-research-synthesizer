# Copy to .env and fill in values before running
GROQ_API_KEY=
TAVILY_API_KEY=
ARS_MODEL=openai/gpt-oss-120b
# Optional: override cache and retrieval settings via env prefix used in README
ARS_SEARCH_MAX_RESULTS=3
ARS_RETRIEVAL_MAX_SOURCES=8
